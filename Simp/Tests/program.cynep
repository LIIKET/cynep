var y = 321;
var z = y;
var x = 123 + 456 * 2;

z = y;
y = 5;

type animal = {
	name;
	properties;
}

type animalproperties = {
	carnivore;
	speed;
}

type player = {
	positionx;
	positiony;
	pet;
}

var playerOne = alloc(player, 5 + 10);
playerOne.pet = alloc(animal);
playerOne.pet.properties = alloc(animalproperties);

playerOne.pet.name = 5 * 10 + y;
playerOne.pet.properties.speed = 200;

var f = null;

z
