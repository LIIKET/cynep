var x = 123 + 456 * 2;
var y = 321;

type animal = {
	name;
	sounds;
}

type sounds = {
	angry;
}

type player = {
	positionx;
	positiony;
	pet;
}

var playerOne = alloc(player);
playerOne.pet = alloc(animal);
playerOne.pet.sounds = alloc(sounds);


playerOne.pet.name = 5 * 10 + y;
playerOne.pet.sounds = 10;



playerOne.pet.sounds